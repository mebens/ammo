require(ammo.path .. ".audio")
require(ammo.path .. ".tweens")
input = require(ammo.path .. ".input")
assets = require(ammo.path .. ".assets")
ammo.db = require(ammo.path .. ".debug")
if love.physics then require(ammo.path .. ".physics") end
